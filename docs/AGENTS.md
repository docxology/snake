# AGENTS.md — docs/ (topic split)

Subdirs: `algorithm/` (canonical form, fitness, pruned BFS, priming), `api/` (per-module API docs), `architecture/` (data structures, memory, package structure), `diagrams/` (flow/data/tree), `guides/` (usage, performance tuning, visualization), `manuscript/` (paper status), `theory/` (complexity, Hamming distance, hypercube graphs, constraints). Loose files: index.md, snake_paper.md, snake_plan.md, getting-started.md, IMPLEMENTATION_SUMMARY.md, TEST_SUITE_REPORT.md, VERIFICATION_COMPLETE.md.
Gotcha: documentation is split by topic, not by module — the same module appears under algorithm/, api/, and architecture/ with different concerns; start from index.md.
