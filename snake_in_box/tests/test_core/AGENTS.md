# AGENTS.md — tests/test_core/

Per-module tests for core/: test_hypercube.py (bitmap), test_transitions.py (conversions), test_validation.py (constraints), test_snake_node.py (node ops). Run: `python -m pytest tests/test_core -q` from snake_in_box/.
