# snake_in_box/tests — Test Suite

Run one directory per pytest invocation: `pytest snake_in_box/tests/test_core -v` (or the whole tree via `snake_in_box/scripts/run_tests.py`).

- Top-level: `test_analysis.py`, `generate_test_outputs.py`
- Mirrored subpackages: `test_core/`, `test_search/`, `test_benchmarks/`, `test_utils/`
