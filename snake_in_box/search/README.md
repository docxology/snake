# snake_in_box/search — Search Algorithms

See also `AGENTS.md`.

- `bfs_pruned.py` — heuristically-pruned BFS (main algorithm)
- `fitness.py` — fitness evaluation
- `priming.py` — priming/lift strategy
- `parallel.py` — parallel search
