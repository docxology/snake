# snake_in_box/benchmarks — Known Snakes & Performance

See also `AGENTS.md`.

- `known_snakes.py` — known record snakes/lengths per dimension (e.g. 2854 in 13D) and validation
- `performance.py` — performance benchmarking helpers
